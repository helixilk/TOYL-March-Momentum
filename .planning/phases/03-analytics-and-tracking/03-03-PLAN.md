---
phase: 03-analytics-and-tracking
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - index.html
autonomous: false

user_setup:
  - service: google-tag-manager
    why: "Tag management container — loads GA4, Meta Pixel, LinkedIn tags"
    env_vars: []
    dashboard_config:
      - task: "Create GTM container (or use existing) and provide Container ID (GTM-XXXXXXX format)"
        location: "tagmanager.google.com -> Admin -> Container Settings"
      - task: "Create GA4 Configuration tag (All Pages trigger, Measurement ID: G-XXXXXXXXXX)"
        location: "GTM -> Tags -> New -> Google Analytics: GA4 Configuration"
      - task: "Create Meta Pixel base tag (All Pages trigger)"
        location: "GTM -> Tags -> New -> Community Templates -> search 'Facebook Pixel'"
      - task: "Create LinkedIn Insight Tag (All Pages trigger)"
        location: "GTM -> Tags -> New -> Community Templates -> search 'LinkedIn'"
      - task: "Create custom event trigger for 'cta_click' and GA4 Event tag firing on it"
        location: "GTM -> Triggers -> New -> Custom Event -> event name: cta_click"
      - task: "Create custom event trigger for 'form_submit' and GA4 Event tag firing on it"
        location: "GTM -> Triggers -> New -> Custom Event -> event name: form_submit"
      - task: "Publish the GTM container (not just Save)"
        location: "GTM -> Submit -> Publish"
  - service: google-analytics-4
    why: "Pageview and event analytics"
    env_vars: []
    dashboard_config:
      - task: "Provide GA4 Measurement ID (G-XXXXXXXXXX format)"
        location: "analytics.google.com -> Admin -> Data Streams -> Web -> Measurement ID"
  - service: meta-pixel
    why: "Meta/Facebook ad retargeting"
    env_vars: []
    dashboard_config:
      - task: "Provide Meta Pixel ID"
        location: "Meta Events Manager -> Data Sources -> Pixel -> Settings -> Pixel ID"
  - service: linkedin-insight-tag
    why: "LinkedIn ad retargeting"
    env_vars: []
    dashboard_config:
      - task: "Provide LinkedIn Partner ID"
        location: "LinkedIn Campaign Manager -> Account Assets -> Insight Tag -> Partner ID"

must_haves:
  truths:
    - "GTM container ID in index.html is a real ID, not a placeholder"
    - "GA4 reports a pageview for a test visit in realtime"
    - "Meta Pixel fires on page load (verified by Meta Pixel Helper extension)"
    - "LinkedIn Insight Tag fires on page load (verified by LinkedIn Tag Helper extension)"
    - "cta_click event appears in GTM Preview when a CTA button is clicked"
    - "form_submit event appears in GTM Preview when waitlist form is submitted"
  artifacts:
    - path: "index.html"
      provides: "Real GTM container ID replacing placeholder"
      contains: "GTM-"
  key_links:
    - from: "index.html GTM snippet"
      to: "GTM container"
      via: "Real container ID"
      pattern: "GTM-[A-Z0-9]{7}"
    - from: "GTM container"
      to: "GA4 property"
      via: "GA4 Configuration tag"
      pattern: "G-[A-Z0-9]+"
---

<objective>
Replace the placeholder GTM container ID with the real one, configure all tags in the GTM dashboard, and verify end-to-end tracking fires correctly.

Purpose: The code changes from Plans 01-02 are inert until a real GTM container ID is in place and the GTM dashboard is configured with GA4, Meta, and LinkedIn tags. This plan closes the loop.

Output: Real analytics IDs deployed, all tags firing, conversion events visible in GTM Preview and GA4 DebugView.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-analytics-and-tracking/03-01-SUMMARY.md
@.planning/phases/03-analytics-and-tracking/03-02-SUMMARY.md
@index.html
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Collect analytics IDs needed for GTM configuration</decision>
  <context>
The GTM snippet in index.html currently uses placeholder ID `GTM-XXXXXXX`. The GTM dashboard needs real IDs for GA4, Meta Pixel, and LinkedIn Insight Tag. These IDs come from external dashboards that only the user can access.

All 4 IDs are required before this plan can proceed:
1. GTM Container ID (format: GTM-XXXXXXX)
2. GA4 Measurement ID (format: G-XXXXXXXXXX)
3. Meta Pixel ID (numeric string)
4. LinkedIn Partner ID (numeric string)
  </context>
  <options>
    <option id="provide-ids">
      <name>Provide all 4 analytics IDs now</name>
      <pros>Plan proceeds immediately to replace placeholder and configure GTM</pros>
      <cons>None</cons>
    </option>
    <option id="skip-some">
      <name>Provide GTM + GA4 only, skip Meta/LinkedIn for now</name>
      <pros>Can ship core analytics without ad platform tags; add Meta/LinkedIn later via GTM dashboard (no code deploy)</pros>
      <cons>Retargeting audiences won't start populating until tags are added</cons>
    </option>
  </options>
  <resume-signal>Provide the IDs in this format: GTM=GTM-XXXXXXX, GA4=G-XXXXXXXXXX, META=1234567890, LINKEDIN=1234567</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder GTM container ID with real ID</name>
  <files>index.html</files>
  <action>
Replace BOTH occurrences of `GTM-XXXXXXX` in index.html with the real GTM Container ID provided by the user in the checkpoint above.

There are exactly 2 occurrences:
1. In the `<head>` script snippet
2. In the `<body>` noscript iframe src

Use find-and-replace: `GTM-XXXXXXX` -> `GTM-{real_id}`

Do NOT modify anything else in index.html.
  </action>
  <verify>
- Grep index.html for `GTM-XXXXXXX` — should return 0 matches
- Grep index.html for the real GTM ID — should return exactly 2 matches
- `npm run build` succeeds
  </verify>
  <done>
- Both GTM placeholder IDs replaced with real container ID
- Build succeeds
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete analytics integration: GTM snippet with real container ID, dataLayer events for cta_click (3 buttons) and form_submit (waitlist), ready for GTM dashboard configuration.
  </what-built>
  <how-to-verify>
**Pre-requisite:** Configure the GTM dashboard with the tags listed in user_setup above, then Publish the container.

**Verification steps:**

1. Run `npm run dev` and open http://localhost:5173 in Chrome
2. Install Google Tag Assistant extension if not present — open it and confirm the GTM container fires on page load
3. Open GTM Preview mode (tagmanager.google.com -> Preview) connected to localhost:5173
4. Verify in GTM Preview: "Container Loaded" event appears with your GTM container ID
5. Check GA4 Realtime report (analytics.google.com -> Reports -> Realtime) — a pageview should appear within seconds
6. Install Meta Pixel Helper extension — confirm the Pixel fires on page load
7. Install LinkedIn Insight Tag Helper extension — confirm the tag fires on page load
8. In GTM Preview, click any "JOIN FOR $27" / "PRACTICE WITH US" / "SECURE MY SPOT" button — verify a `cta_click` event appears in the GTM Preview event stream
9. Fill out and submit the waitlist form — verify a `form_submit` event appears in the GTM Preview event stream
10. Check GA4 DebugView (analytics.google.com -> Admin -> DebugView) — confirm both `cta_click` and `form_submit` events appear

**Expected:**
- All 4 tracking systems fire on page load (GTM, GA4 pageview, Meta Pixel, LinkedIn Insight)
- CTA clicks produce cta_click events with cta_location parameter
- Waitlist submission produces form_submit event with form_name parameter
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which checks failed</resume-signal>
</task>

</tasks>

<verification>
- GTM container loads with real ID (not placeholder)
- GA4 pageview visible in realtime report
- Meta Pixel fires (Pixel Helper confirms)
- LinkedIn Insight Tag fires (Tag Helper confirms)
- cta_click event visible in GTM Preview on button click
- form_submit event visible in GTM Preview on successful waitlist submission
</verification>

<success_criteria>
1. Real GTM container ID in index.html (both head and noscript)
2. GA4 Configuration tag fires on all pages — pageview visible in GA4 Realtime
3. Meta Pixel and LinkedIn Insight Tag fire on page load — confirmed by browser extensions
4. cta_click custom event triggers in GTM Preview when any CTA button is clicked
5. form_submit custom event triggers in GTM Preview on successful waitlist submission
6. All events forwarded to GA4 DebugView
</success_criteria>

<output>
After completion, create `.planning/phases/03-analytics-and-tracking/03-03-SUMMARY.md`
</output>
