---
phase: 02-espocrm-lead-capture
plan: 04
type: tdd
wave: 3
depends_on:
  - "02-03"
files_modified:
  - src/tests/WaitlistForm.test.tsx
  - e2e/waitlist.spec.ts
autonomous: true

must_haves:
  truths:
    - "Unit tests cover all four WaitlistForm states: renders form in idle, shows loading state, shows success message, shows error message"
    - "Unit tests pass without a running server or real network calls"
    - "E2E test submits the form end-to-end with the API mocked via page.route() and asserts the success message"
    - "The full test suite passes: `npm test` exits 0"
  artifacts:
    - path: "src/tests/WaitlistForm.test.tsx"
      provides: "Unit tests for WaitlistForm component"
      contains: "vi.stubGlobal"
    - path: "e2e/waitlist.spec.ts"
      provides: "E2E test for waitlist form flow"
      contains: "page.route"
  key_links:
    - from: "src/tests/WaitlistForm.test.tsx"
      to: "src/components/WaitlistForm.tsx"
      via: "import WaitlistForm"
      pattern: "WaitlistForm"
    - from: "e2e/waitlist.spec.ts"
      to: "/.netlify/functions/submit-lead"
      via: "page.route intercept"
      pattern: "page\\.route"
---

<objective>
Write unit tests for WaitlistForm and an E2E test for the full waitlist flow. Unit tests use vi.stubGlobal("fetch") to isolate the component from real network calls. The E2E test uses page.route() to intercept the function call and return a mock 200 response.

Purpose: Test coverage locks in the WaitlistForm contract so regressions are caught before they reach production. The TDD cycle also confirms the component API is testable and accessible.

Output: src/tests/WaitlistForm.test.tsx and e2e/waitlist.spec.ts, with `npm test` passing green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-espocrm-lead-capture/02-RESEARCH.md
@.planning/phases/02-espocrm-lead-capture/02-03-SUMMARY.md
@src/tests/Button.test.tsx
@e2e/smoke.spec.ts
@playwright.config.ts
</context>

<feature>
  <name>WaitlistForm unit and E2E tests</name>
  <files>src/tests/WaitlistForm.test.tsx, e2e/waitlist.spec.ts</files>
  <behavior>
    Unit test cases (all using vi.stubGlobal("fetch", vi.fn())):
    - renders first name, last name, email inputs and a submit button in idle state
    - submit button shows "Joining..." and is disabled while fetch is in flight
    - shows success confirmation text after fetch resolves with { ok: true, json: () => { success: true } }
    - shows error message after fetch resolves with { ok: false, json: () => { error: "CRM error" } }
    - shows fallback error message when fetch rejects (network error)

    E2E test cases (page.route mocking):
    - fill all three fields + click submit -> mock 200 -> assert success message visible
    - (optional) fill and submit with mock 500 -> assert error message visible
  </behavior>
  <implementation>
    Follow the existing test file patterns in src/tests/ — they import from @testing-library/react and use describe/it/expect from vitest.

    For the E2E test, register the page.route() intercept BEFORE page.goto('/') so it is active when the form loads. The WaitlistForm will be embedded in the page after Plan 05 wires it into App.tsx. If App.tsx integration has not yet happened when these tests run, write the E2E test against the full page and expect the form to be present — the test will pass once Plan 05 completes.

    The Playwright webServer config in playwright.config.ts spawns `npm run dev` with `GEMINI_API_KEY=placeholder-key-ci`. No changes needed to playwright.config.ts.
  </implementation>
</feature>

<verification>
- RED: write tests first, run `npm run test:unit` — tests must fail with "Cannot find module" or component-not-found errors
- GREEN: tests pass after WaitlistForm exists (it already does from Plan 03)
- `npm run test:unit` exits 0 with all WaitlistForm tests passing
- `npm run test:e2e` exits 0 with waitlist.spec.ts passing
- `npm test` exits 0 (full suite green)
</verification>

<success_criteria>
`npm test` exits 0. Unit tests cover all four WaitlistForm states. The E2E test submits the form with a mocked API response and asserts the success confirmation. No real network calls to EspoCRM are made during tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-espocrm-lead-capture/02-04-SUMMARY.md`
</output>
