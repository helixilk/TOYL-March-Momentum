---
phase: 02-espocrm-lead-capture
plan: 05
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - src/App.tsx
autonomous: true

must_haves:
  truths:
    - "A waitlist section is visible on the page with the WaitlistForm component rendered inside it"
    - "The section has an anchor id so it can be linked from the nav or CTAs"
    - "The form is positioned after the hero section and before (or near) the final CTA"
    - "No existing page sections are removed or broken by this change"
  artifacts:
    - path: "src/App.tsx"
      provides: "Application shell with WaitlistForm section integrated"
      contains: "WaitlistForm"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/WaitlistForm.tsx"
      via: "import WaitlistForm"
      pattern: "WaitlistForm"
---

<objective>
Integrate WaitlistForm into App.tsx by adding a dedicated waitlist section to the page. This makes the form visible to visitors and wires the E2E test path so Playwright can find the form on page load.

Purpose: The form component and service exist but are not visible to users until they are mounted in the page. This plan completes the user-facing integration.

Output: Updated src/App.tsx with a waitlist section containing WaitlistForm.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-espocrm-lead-capture/02-03-SUMMARY.md
@src/App.tsx
@src/components/WaitlistForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add waitlist section to App.tsx</name>
  <files>src/App.tsx</files>
  <action>
    Import WaitlistForm at the top of App.tsx:
    ```typescript
    import WaitlistForm from './components/WaitlistForm';
    ```

    Add a waitlist section to the page JSX. Place it between the "How it Works" section and the "Daily Motivation" section (i.e., after the protocol breakdown and before the DailyIntention component). This positioning is natural — visitors have learned what the program is and are ready to sign up before being asked to pay.

    The section structure:
    ```jsx
    <section id="waitlist" className="py-24 bg-viridian-soft">
      <div className="max-w-2xl mx-auto px-6">
        <div className="text-center mb-12">
          <h2 className="text-4xl md:text-5xl font-serif text-viridian-dark mb-4">
            Join the Waitlist
          </h2>
          <p className="text-lg text-[#576574] leading-relaxed">
            Be the first to know when the next cohort opens. Enter your details and we'll reach out directly.
          </p>
        </div>
        <WaitlistForm />
      </div>
    </section>
    ```

    Also add a "Join Waitlist" nav link in the existing `<nav>` alongside the other nav items (How it Works, About, Benefits), pointing to `#waitlist`. This gives visitors a direct path to the form from the top of the page.

    Do NOT remove or modify any existing sections. Do NOT change the existing CTA buttons (they still point to STRIPE_PLACEHOLDER_URL). This section is additive only.

    Verify the file still compiles after changes:
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <verify>
    1. `npx tsc --noEmit` passes with no errors
    2. `npm run dev` starts and the page renders the waitlist section with the form visible
    3. Scrolling to `#waitlist` shows the form with three inputs and a submit button
    4. All existing sections (hero, benefits, how-it-works, FAQ, CTA, footer) are still present and functional
  </verify>
  <done>
    - WaitlistForm is imported and rendered in a `<section id="waitlist">` in App.tsx
    - "Join Waitlist" link appears in the nav pointing to #waitlist
    - `npx tsc --noEmit` passes
    - `npm run dev` shows the waitlist section on the page
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `grep "WaitlistForm" src/App.tsx` returns the import line and the JSX usage
- `grep "id=\"waitlist\"" src/App.tsx` confirms the section anchor exists
- `npm run test:unit` still passes (existing tests unaffected)
</verification>

<success_criteria>
WaitlistForm is visible on the page inside a `#waitlist` section. The nav has a link to it. All existing sections are intact. TypeScript compiles cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-espocrm-lead-capture/02-05-SUMMARY.md`
</output>
