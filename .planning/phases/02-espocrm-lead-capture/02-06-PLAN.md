---
phase: 02-espocrm-lead-capture
plan: 06
type: execute
wave: 4
depends_on:
  - "02-02"
  - "02-04"
  - "02-05"
autonomous: false

must_haves:
  truths:
    - "Submitting the form with real data creates a Lead in EspoCRM visible in the admin dashboard"
    - "The new Lead has source set to Web Site (with space)"
    - "The ESPOCRM_API_KEY is not visible in any browser network request or the page source"
    - "The full test suite passes before this checkpoint is reached"
    - "If a UTM parameter was in the URL, the Lead description field contains it"
  artifacts:
    - path: "netlify/functions/submit-lead.mts"
      provides: "Deployed and working serverless function"
    - path: "src/components/WaitlistForm.tsx"
      provides: "Working form with 4 states"
    - path: "src/tests/WaitlistForm.test.tsx"
      provides: "Passing unit tests"
    - path: "e2e/waitlist.spec.ts"
      provides: "Passing E2E tests"
  key_links:
    - from: "Browser form submit"
      to: "EspoCRM Lead record"
      via: "WaitlistForm -> waitlistService -> submit-lead.mts -> EspoCRM API"
      pattern: "end-to-end"
---

<objective>
Verify the complete end-to-end lead capture flow: automated test suite passes, a real form submission creates a Lead in EspoCRM, the API key is confirmed absent from browser traffic, and the EspoCRM workflow log confirms the automated email sequence triggered.

Purpose: This is the final quality gate before the feature/espocrm-integration PR is ready to merge. All five success criteria from the phase roadmap must be satisfied.

Output: Confirmed working lead capture, clean test suite, PR ready to merge.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm green</name>
  <files></files>
  <action>
    Run the full test suite:
    ```bash
    npm test
    ```

    This runs both `vitest run` (unit) and `playwright test` (E2E). All tests must pass before proceeding to the checkpoint.

    If any tests fail, fix the failures before continuing. Common failure modes:
    - E2E waitlist.spec.ts: form not found on page — check that Plan 05 App.tsx integration is complete
    - Unit WaitlistForm.test.tsx: loading state not found — check aria-busy attribute on submit button
    - Any existing test regressions: check that App.tsx changes in Plan 05 did not break existing E2E specs
  </action>
  <verify>`npm test` exits 0 with no failures</verify>
  <done>`npm test` exits 0 — unit and E2E suites both green</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete EspoCRM lead capture feature:
    - Netlify infrastructure (netlify.toml, @netlify/vite-plugin, .env.local.example)
    - Serverless proxy function (netlify/functions/submit-lead.mts)
    - WaitlistForm component with 4 states + waitlistService
    - Waitlist section in App.tsx with nav link
    - Unit tests (WaitlistForm.test.tsx) and E2E tests (waitlist.spec.ts)
  </what-built>
  <how-to-verify>
    **Step 1 — Confirm dev server is running:**
    Run `npm run dev`, visit http://localhost:3000, scroll to the "Join the Waitlist" section (or click the nav link).

    **Step 2 — Submit a real test lead:**
    Fill in the form with test data:
    - First Name: Test
    - Last Name: Lead
    - Email: your-real-email@example.com (use a real address if you want to receive the automated email sequence)

    Click "Join the Waitlist". You should see:
    - Button shows "Joining..." and is disabled while in flight
    - Form is replaced by a success confirmation message (e.g. "You're on the list!")

    **Step 3 — Verify Lead in EspoCRM:**
    Open your EspoCRM dashboard and navigate to Leads. Find the record just created. Confirm:
    - [ ] First Name: Test
    - [ ] Last Name: Lead
    - [ ] Email: the address you entered
    - [ ] Source: Web Site (with space — if blank, the enum value was wrong)
    - [ ] Status: New (or whatever EspoCRM defaulted to)

    **Step 4 — Confirm API key is not exposed:**
    In your browser, open DevTools > Network tab. Find the request to `/.netlify/functions/submit-lead`. Inspect the request payload and response. Confirm:
    - [ ] The request payload contains firstName, lastName, email (no API key)
    - [ ] The response body is `{"success":true}` (no API key echoed back)
    - [ ] There is no `ESPOCRM_API_KEY` visible anywhere in the Network tab

    **Step 5 — Verify automated email sequence (CRM-05):**
    In EspoCRM, open the Test Lead record. Look for a "Workflow Log" or "Stream" section showing workflow activity. Confirm:
    - [ ] An automated workflow triggered after the Lead was created
    - [ ] (If the sequence sends an email immediately) Check that the email address received a welcome/nurture email

    If the EspoCRM workflow is not yet configured (it requires admin setup in EspoCRM, not code), note this as a blocker — the code is complete but CRM admin configuration is needed before CRM-05 is satisfied.

    **Step 6 — UTM parameter test (optional but recommended):**
    Visit http://localhost:3000?utm_source=test&utm_medium=manual&utm_campaign=phase2-verify
    Submit the form again with different test data. In EspoCRM, verify the new Lead's Description field contains:
    `utm_source=test utm_medium=manual utm_campaign=phase2-verify`
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass and a real Lead is visible in EspoCRM.
    Or describe any issues found (e.g. "source field is blank", "workflow did not trigger", "loading state not visible").
  </resume-signal>
</task>

</tasks>

<verification>
All five phase success criteria from ROADMAP.md:
1. Waitlist form visible on page, accepts fields, submits without page reload — confirmed by Step 1-2
2. Lead appears in EspoCRM with source "Web Site" and UTM params — confirmed by Step 3 and 6
3. API key absent from all browser traffic — confirmed by Step 4
4. Form communicates state: loading indicator, success confirmation, error message — confirmed by Step 2
5. Automated email sequence triggered (visible in EspoCRM workflow log) — confirmed by Step 5
</verification>

<success_criteria>
All five roadmap success criteria are satisfied. `npm test` exits 0. A real Lead is visible in EspoCRM. The API key is confirmed absent from browser traffic. The feature/espocrm-integration PR is ready for merge.
</success_criteria>

<output>
After completion, create `.planning/phases/02-espocrm-lead-capture/02-06-SUMMARY.md`
</output>
